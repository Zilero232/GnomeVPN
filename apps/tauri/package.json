{
  "name": "@gnomevpn/tauri",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "icon": "tauri icon icon.manifest.json -o icons && node ./scripts/android-foreground.mjs",
    "predev": "bun run kill && node ./scripts/sync-bin.mjs && node ./scripts/build-service.mjs && bun run icon",
    "dev": "tauri dev",
    "prebuild": "node ./scripts/sync-bin.mjs && node ./scripts/build-service.mjs --release && bun run icon",
    "build": "tauri build",
    "android:init": "bun run icon && bun --env-file .env tauri android init && node ./scripts/setup-android-libs.mjs",
    "android:dev": "bun run icon && node ./scripts/setup-android-libs.mjs && bun --env-file .env tauri android dev",
    "android:build": "bun run icon && node ./scripts/setup-android-libs.mjs && bun --env-file .env tauri android build --apk",
    "service:build": "node ./scripts/build-service.mjs",
    "signer:sign": "tauri signer sign",
    "signer:generate": "tauri signer generate -w ~/.tauri/gnomevpn.key --ci",
    "kill": "fkill --force --silent gnomevpn.exe",
    "info": "tauri info"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
