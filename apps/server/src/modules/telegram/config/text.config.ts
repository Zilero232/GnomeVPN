export const TEXT_TOKEN = {
  platforms: '{platforms}',
  count: '{count}',
  price: '{price}',
  date: '{date}',
  plan: '{plan}',
  site: '{site}'
} as const;

export const AUTO_RENEW_CHOICE = {
  on: 'on',
  off: 'off'
} as const;

export const DEVICE_CHOICES = [1, 2, 3] as const;

export const NEW_LINE = String.fromCharCode(10);
